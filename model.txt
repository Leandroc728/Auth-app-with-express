Please, before run the app, create the database model

CREATE TABLE users(id int AUTO_INCREMENT, name varchar(80), username varchar(80), password varchar(255), created_at varchar(50), PRIMARY KEY(id));